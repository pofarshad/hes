<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Updated to remove references to older Material attributes (colorPrimary, etc.)
        which are not defined in Material3.
        Compose-based theming handles colors from code.
    -->
    <style name="Theme.VpnResellerApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Set a minimal XML-based definition to avoid resource linking errors. -->
        <!--
            If needed, define system UI colors below,
            but ensure consistency with Compose-based theming.
        -->
        <item name="android:statusBarColor">?android:attr/statusBarColor</item>
        <item name="android:navigationBarColor">?android:attr/navigationBarColor</item>
    </style>
</resources>
